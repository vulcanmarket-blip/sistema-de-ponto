{
  "name": "sistema-de-ponto",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@prisma/client": "^5.17.0", // Mantenha a sua versão ou atualize se quiser
    "bcryptjs": "^2.4.3",
    "next": "16.0.0", // Use a versão exata do Next.js instalada
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "latest", // Atualizado
    "autoprefixer": "latest",        // Atualizado
    "eslint": "^9",                  // Mantido (ou use a sua versão)
    "eslint-config-next": "16.0.0",  // Mantido (ou use a sua versão)
    "postcss": "latest",             // Atualizado
    "prisma": "^5.17.0",             // Mantenha a sua versão do prisma
    "tailwindcss": "latest"          // Atualizado
  }
}
